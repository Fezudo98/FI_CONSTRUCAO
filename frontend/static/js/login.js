document.getElementById('login-form').addEventListener('submit',async(e)=>{e.preventDefault();const a=document.getElementById('alert'),b=e.currentTarget.querySelector('button');a.hidden=true;b.disabled=true;b.textContent='Entrando…';try{const r=await fetch('/api/auth/login',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email:document.getElementById('email').value.trim(),password:document.getElementById('password').value})});const d=await r.json();if(!r.ok){a.textContent=d.error||'Confira seus dados e tente novamente.';a.hidden=false;return}location.href='/index.html'}catch(_){a.textContent='Servidor indisponível. Verifique a conexão e tente novamente.';a.hidden=false}finally{b.disabled=false;b.textContent='Entrar'}});
