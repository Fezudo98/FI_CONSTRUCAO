async function loadLowStock(){const b=document.querySelector('#low-stock-table tbody'),s=document.getElementById('low-stock-summary');try{const r=await fetch('/api/reports/low-stock');if(!r.ok)throw 0;const{products}=await r.json();b.replaceChildren();s.textContent=products.length?products.length+' produto(s) abaixo do mínimo':'Nenhuma reposição urgente';if(!products.length){const c=b.insertRow().insertCell();c.colSpan=3;c.className='muted';c.textContent='Todos os produtos estão dentro do saldo mínimo.';return}products.forEach(p=>{const row=b.insertRow();[p.name,p.current_stock,p.min_stock].forEach(v=>row.insertCell().textContent=v)})}catch(_){s.textContent='Não foi possível consultar agora';b.innerHTML='<tr><td colspan="3" class="muted">Atualize a página para tentar novamente.</td></tr>'}}loadLowStock();
